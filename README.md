# 🧮 Calculadora Simples

Um pacote Python de exemplo criado para demonstrar os conceitos básicos de empacotamento, distribuição e instalação de código Python reutilizável utilizando as ferramentas `setuptools` e `pip`.

Este projeto foi desenvolvido como um exercício prático para substituir módulos auxiliares customizados (como `xg_helpers`) por código Python empacotado e instalável.

## ✨ Funcionalidades

O pacote `calculadora_simples` oferece funções básicas para cálculo de área de formas geométricas:

* `area_circulo(raio)`: Calcula a área de um círculo.
* `area_quadrado(lado)`: Calcula a área de um quadrado.

## 🚀 Instalação

### 1. Requisitos

É necessário ter o Python (versão 3.8+) e o `pip` instalados no seu ambiente.

### 2. Instalação via Pip (Recomendado)

Se este pacote fosse publicado no PyPI (Python Package Index), a instalação seria simples:

```bash
pip install calculadora_simples
